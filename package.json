{
  "name": "carpoolapp",
  "version": "1.0.0",
  "description": "CarPooling application",
  "main": "src/main.js",
  "scripts": {
    "init-env": "ln -s ./public/images ./images; touch secret.json",
    "start": "NODE_ENV=production webpack-dev-server -d --colors --hot --inline --progress",
    "start-dev": "NODE_ENV=dev webpack-dev-server -d --colors --hot --inline --progress",
    "pack": "npm run build && rm -rf ./package; cp -r ./public ./package; cp ./dist/carpool.js ./package/",
    "build": "NODE_ENV=production webpack -p --colors --progress",
    "deploy": "firebase deploy",
    "open": "firebase open",
    "packdep": "npm run pack && npm run deploy"
  },
  "author": "Mathieu ANCELIN <mathieu.ancelin@gmail.com> (http://reactivecouchbase.org/)",
  "license": "MIT",
  "dependencies": {
    "babel-polyfill": "^6.1.19",
    "firebase": "^4.12.1",
    "hammerjs": "^2.0.4",
    "moment": "^2.10.6",
    "react": "^16.3.1",
    "react-dom": "^16.3.1"
  },
  "devDependencies": {
    "babel-cli": "^6.24.1",
    "babel-core": "^6.25.0",
    "babel-loader": "^7.1.1",
    "babel-preset-es2015": "^6.24.1",
    "babel-preset-react": "^6.24.1",
    "babel-preset-stage-0": "^6.24.1",
    "firebase-tools": "^2.1.0",
    "webpack": "^3.5.3",
    "webpack-dev-server": "^2.7.1"
  }
}
